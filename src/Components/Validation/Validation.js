import React from 'react';
//Styles
import './validation.scss';

const Validation = () => {
    return (
        <span className="validation">
            Invalid entry
        </span>
    );
};

export default Validation;
